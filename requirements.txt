python-telegram-bot[job-queue]
httpx
pytz
python-dotenv
numpy

# AI provider SDKs — you only need the one matching AI_PROVIDER in .env,
# but all three are listed so switching providers never requires a reinstall.
google-genai
openai
anthropic
